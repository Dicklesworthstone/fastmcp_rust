# Database
*.db
*.db-shm
*.db-wal

# Lock files
*.lock
.write.lock

# Temporary
last-touched
*.tmp

# Local history backups
.br_history/

# bv (beads viewer) lock file
.bv.lock

# Crash-recovery, backup, and local-state artifacts (never commit)
*.bak
beads.db-*
beads.base.jsonl
.br_recovery/
.local_version
recovery_*/
_tmp_*
.write.lock.stale-*
*-fsqlite-ns-gate
*-fsqlite-ns-use
*.vacuum-wal-cert*
*.fsqlite-migration-state
