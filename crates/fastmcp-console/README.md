# fastmcp-console

Rich console output for FastMCP servers.

This crate renders human-friendly output to stderr while keeping stdout reserved
for JSON-RPC messages so agents can parse the protocol reliably.

## Status

Early development.
