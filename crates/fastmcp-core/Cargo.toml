[package]
name = "fastmcp-core"
version.workspace = true
edition.workspace = true
license-file.workspace = true
rust-version.workspace = true
description = "Core types and context for FastMCP"
repository.workspace = true
homepage.workspace = true
readme.workspace = true
documentation = "https://docs.rs/fastmcp-core"
keywords = ["mcp", "context", "cancellation", "budget", "asupersync"]
categories = ["development-tools", "network-programming"]

[dependencies]
asupersync.workspace = true
serde.workspace = true
serde_json.workspace = true
log.workspace = true
# FND-01 CORE-*: inherit exact root pins (no local version/feature rewrites)
getrandom = { workspace = true }
hmac = { workspace = true }
sha2 = { workspace = true }
url = { workspace = true }
zeroize = { workspace = true }

[dev-dependencies]
# Unit tests exercise asupersync's deliberately test-only context constructors.
# This edge is absent from the published production dependency graph.
asupersync = { workspace = true, features = ["test-internals"] }

[lints]
workspace = true
