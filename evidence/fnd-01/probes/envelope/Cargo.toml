[package]
name = "fastmcp-fnd01-envelope-probe"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
chacha20poly1305 = { version = "=0.11.0", default-features = false, features = ["alloc", "zeroize"] }

[lib]
path = "src/lib.rs"

[workspace]
resolver = "3"
