[package]
name = "fastmcp-fnd01-redis-probe"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
redis = { version = "=1.6.0", default-features = false, features = ["acl", "script"] }

[lib]
path = "src/lib.rs"

[workspace]
resolver = "3"
